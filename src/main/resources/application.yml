spring:
  datasource:
    url: jdbc:mysql://192.168.43.85/jpa
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      # 定义数据表的生成策略
      # update 更新或创建数据表结构
      ddl-auto: update
    # 控制台打印sql
    show-sql: true
